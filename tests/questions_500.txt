列出目前目錄有哪些檔案
幫我讀一下 Cargo.toml
建立一個 test.txt，內容寫 hello world
幫我看一下系統記憶體和磁碟空間
Docker 有在跑嗎？
ollama 有在跑嗎？
PM2 上面有哪些服務？
幫我 ping 一下 https://google.com 看通不通
目前有哪些 process 在跑？
幫我看一下信箱有沒有新信
看一下 README.md 的內容
系統 uptime 多久了
幫我建一個 notes.md，內容寫今天的日期
這個資料夾多大
nginx 有在跑嗎？
幫我讀一下 .gitignore
redis 有在跑嗎？
幫我 ping https://github.com
列出 src/ 底下的檔案
看一下磁碟還剩多少空間
List all files in the current directory
Read the Cargo.toml file
Create a file called hello.txt with "hello world" inside
Check system memory and disk usage
Is Docker running?
Is ollama running?
What services are on PM2?
Ping https://google.com
What processes are currently running?
Check my inbox for new emails
幫我讀 config.example.toml
看一下 Dockerfile 內容
postgres 有在跑嗎？
幫我建一個 tmp/ 資料夾
zeabur.json 裡面寫什麼
看看 docs/ 底下有什麼
幫我 ping localhost:18789
系統現在幾點
幫我建一個 .env 檔案，內容是 DEBUG=true
看一下 Cargo.lock 有多大
Show me the project structure
Read the Dockerfile
What's in the docs/ directory?
Is postgres running?
Create a folder called tmp
Ping localhost:18789
What time is it on the system?
Create a .env file with DEBUG=true
How big is Cargo.lock?
Read the .gitignore file
src 底下有什麼 rust 檔案？
幫我把 README.md 裡的 MIT 改成 Apache-2.0
跑一下 cargo build 看有沒有錯
git status 看一下目前狀態
找一下專案裡哪裡有用到 SessionStore
幫我刪除 test.txt
看一下 git log 最近 5 筆
幫我在 src 底下建立 utils 資料夾
這個專案用了哪些 crate？
幫我把 hello.py 裡的 print 改成 println
找一下哪些檔案有用到 tokio
git branch 看目前有哪些分支
幫我把 Cargo.toml 裡的 description 改成 "AI Agent"
跑 cargo check 看有沒有問題
找出所有 .toml 檔案
幫我建一個 scripts/ 資料夾
看一下最新一筆 git commit 是什麼
找出 config.rs 裡面有幾個 struct
幫我把 .gitignore 加上 *.log
src/main.rs 的前 20 行是什麼
列出 src/tools/ 底下所有檔案
找一下哪裡有用到 anyhow
幫我執行 echo hello
看一下 target/ 資料夾多大
git remote -v 看遠端設定
找出所有有 pub fn 的行
幫我複製 README.md 到 README.bak
看一下 src/agent/ 底下有什麼
找出哪些檔案 import 了 serde
幫我建立 test.rs 內容是 fn main() {}
Find all .rs files in src/
Change MIT to Apache-2.0 in README.md
Run cargo build
Show git status
Search for "SessionStore" in the project
Delete test.txt
Show last 5 git commits
Create a utils/ folder in src/
What crates does this project use?
Find all files that use tokio
Show current git branches
Change the description in Cargo.toml
Run cargo check
Find all .toml files
Create a scripts/ directory
Show the latest git commit
Find how many structs are in config.rs
Add *.log to .gitignore
Show the first 20 lines of src/main.rs
List files in src/tools/
找一下哪裡有 TODO 註解
幫我執行 whoami
看一下 src/session/memory.rs 有多少行
找出所有 async fn
幫我把 test.rs 刪掉
看一下 git diff 有什麼改動
找出所有 use crate:: 的行
幫我建立 data/ 資料夾
看一下系統的 hostname
找出 src/channels/ 底下所有檔案
Find all TODO comments
Run whoami
How many lines in src/session/memory.rs?
Find all async functions
Delete test.rs
Show git diff
Find all "use crate::" lines
Create a data/ directory
Show system hostname
List files in src/channels/
找一下有用到 Arc<Mutex 的地方
幫我看 src/tools/executor.rs 的結構
執行 date 指令
找出所有 impl 區塊
幫我建一個 Makefile 內容是 build: cargo build --release
找出 main.rs 裡所有 use 語句
幫我把 scripts/ 刪掉
看一下 /tmp 底下有什麼
找出所有 #[derive 的行
幫我執行 ls -la
幫我看 src/main.rs 有幾行
找到 config.rs 裡面 default_port 是多少
幫我寫一個 hello.rs 內容是 hello world 程式然後編譯它
看一下 target/release/rustclaw 多大
幫我找出所有用到 Arc 的檔案
把 Cargo.toml 的 version 從 0.1.0 改成 0.2.0 然後 commit
跑 cargo test 然後告訴我有沒有失敗的
幫我看一下 port 18789 有沒有被佔用
比較一下 src/config.rs 和 config.example.toml 的設定項目有沒有對齊
幫我備份 src/main.rs 到 src/main.rs.bak
計算 src/ 底下總共有多少行 Rust 程式碼
找出 Cargo.toml 裡所有 features 設定
幫我寫一個 test.sh 腳本，跑 cargo build 然後跑 cargo test
看一下最近 3 天改了哪些檔案
找出哪個 .rs 檔案最大
幫我建一個 docker-compose.yml 跑 redis
看一下 git log 裡有幾個不同的 author
找出所有 error handling（unwrap, expect, ?）的使用
幫我寫一個 benchmark.py 用 requests 打 /health 100 次
比較 gateway/server.rs 和 gateway/connection.rs 的行數
Count total lines of Rust code in src/
Find all features settings in Cargo.toml
Write a test.sh script that runs cargo build then cargo test
Which files were modified in the last 3 days?
Find the largest .rs file
Create a docker-compose.yml for redis
How many different authors in git log?
Find all error handling patterns (unwrap, expect, ?)
Write a benchmark.py that hits /health 100 times
Compare line counts of server.rs vs connection.rs
找出所有 pub struct 並列出它們的名字
幫我寫一個 .editorconfig 設定 4 spaces indent
看一下哪個模組的依賴最多
找出所有 #[cfg(test)] 區塊
幫我建一個 CHANGELOG.md 從 git log 自動產生
檢查有沒有未使用的 import
找出所有 serde_json::json! 的使用位置
幫我寫一個 curl 指令測試 WebSocket 連線
計算每個模組（agent, channels, tools 等）各有幾行
找出所有 .await 的數量
List all pub structs and their names
Create an .editorconfig with 4 spaces indent
Which module has the most dependencies?
Find all #[cfg(test)] blocks
Generate a CHANGELOG.md from git log
Check for unused imports
Find all serde_json::json! usages
Write a curl command to test WebSocket connection
Count lines per module (agent, channels, tools, etc.)
Count all .await occurrences
找出有 unsafe 的地方
幫我寫一個 justfile 替代 Makefile
看一下 Cargo.lock 有多少個不同的 crate
找出所有 tracing::info 的 log 位置
幫我計算 binary 加了 teloxide 後增加了多少
檢查有沒有 FIXME 或 HACK 註解
幫我建一個 .github/workflows/ci.yml 跑 cargo test
找出所有超過 100 行的函式
看一下 sessions.db 的 schema
幫我寫一個 migration 腳本把舊 DB schema 更新
Find any unsafe code
Create a justfile instead of Makefile
How many unique crates in Cargo.lock?
Find all tracing log locations
Check for FIXME or HACK comments
Create .github/workflows/ci.yml for cargo test
Find all functions over 100 lines
Show the sessions.db schema
Count pub vs private functions
List all tool names in executor.rs
幫我產生一個 API 文檔（列出所有 REST endpoint）
找出所有 Clone derive 的 struct
看一下 embed.rs 和 extract.rs 加起來多少行
找出 graph.rs 裡的 SQL 語句
幫我寫一個 seed 腳本往 DB 插入測試資料
比較 telegram.rs 和 discord.rs 的程式碼結構差異
檢查所有 SQL injection 風險（有沒有 string concat 的 SQL）
看一下 MCP client 的程式碼大小
幫我把所有 TODO 整理成一個清單
找出最長的一行程式碼
Generate API documentation listing all REST endpoints
Find all structs with Clone derive
Count lines of embed.rs + extract.rs combined
Find SQL statements in graph.rs
Write a seed script to insert test data into DB
Compare code structure of telegram.rs vs discord.rs
Check for SQL injection risks
How big is the MCP client code?
Compile all TODOs into a list
Find the longest line of code
幫我寫一個 healthcheck.sh 腳本
找出所有 error 型別定義
看一下 cron/mod.rs 裡有幾個 Job
找出哪些函式有超過 5 個參數
幫我整理一下 Cargo.toml 的依賴按字母排序
看看有沒有重複的 import
幫我建一個 examples/ 資料夾放使用範例
找出所有 match 語句
計算 async fn vs sync fn 的比例
幫我把 README 裡的架構圖更新
分析整個 src 目錄的程式碼結構告訴我每個模組負責什麼
找出專案裡所有的 TODO 和 FIXME 註解
幫我建立一個新的 tool src/tools/weather.rs 裡面寫一個取得天氣的函式
檢查所有 Docker container 把掛掉的重啟
掃描專案的安全性問題
幫我寫一個 shell script deploy.sh 把專案編譯並部署
讀取最新的 3 封 email 分類哪些是重要的
找出 runner.rs 裡最長的函式告訴我有幾行
比對 git diff 看最近改了什麼做個摘要
幫我把整個專案打包成 tar.gz
分析 memory.rs 的記憶管理邏輯，畫出流程
幫我重構 extract.rs 把重複的 LLM 呼叫抽成共用函式
檢查整個 src/ 的錯誤處理是否一致（有沒有 unwrap 該用 ? 的）
幫我寫一個壓力測試腳本同時發 10 個請求
分析 Cargo.toml 的依賴哪些可以精簡
幫我把 system_prompt 從 config 讀出來加上記憶內容
看一下 cron 排程器有沒有正確設定
找出所有跨模組的依賴關係
幫我寫一個 Docker multi-stage build 最小化 image
分析記憶系統的 dedup 邏輯是否有 edge case
Analyze the entire src/ directory structure and explain each module
Find all TODO and FIXME comments
Create src/tools/weather.rs with a weather fetching function
Check all Docker containers and restart any that are down
Scan the project for security issues
Write a deploy.sh script to build and deploy
Read latest 3 emails and classify which are important
Find the longest function in runner.rs
Summarize recent git diff changes
Package the entire project as tar.gz
幫我寫一個 integration test 測試 memory add + search
分析 graph.rs 的 soft-delete 邏輯是否正確
找出所有可能的 panic 點
幫我建一個 monitoring dashboard 的 HTML 頁面
分析 WebSocket handshake 流程是否安全
幫我寫一個 migration 把舊版 DB 升級到新版
找出所有 Clone 但不應該 Clone 的大型 struct
幫我寫一個 GitHub Actions workflow 自動測試+部署
分析 email.rs 的 IMAP 連線是否有 timeout 處理
幫我把所有硬編碼的字串抽到 config
Write an integration test for memory add + search
Analyze if graph.rs soft-delete logic is correct
Find all potential panic points
Build a monitoring dashboard HTML page
Analyze WebSocket handshake security
Write a migration script for DB schema upgrade
Find all large structs that shouldn't implement Clone
Write a GitHub Actions CI/CD workflow
Analyze if email.rs IMAP connection has timeout handling
Extract all hardcoded strings to config
幫我做一個 profiling 看哪個函式最慢
分析 MCP client 的錯誤恢復機制
找出所有沒有單元測試的 pub fn
幫我寫一個 Prometheus metrics endpoint
分析 tool executor 的路由邏輯是否有遺漏
幫我建一個 Grafana dashboard JSON
分析記憶系統的 scope 隔離是否有漏洞
幫我寫一個 load test 模擬 100 個 WebSocket 連線
找出所有 magic number 並建議改成常數
幫我分析整個專案的錯誤處理策略
Profile which function is slowest
Analyze MCP client error recovery
Find all pub fn without unit tests
Write a Prometheus metrics endpoint
Analyze tool executor routing for gaps
Build a Grafana dashboard JSON
Analyze memory scope isolation for leaks
Write a load test simulating 100 WebSocket connections
Find all magic numbers and suggest constants
Analyze the project's overall error handling strategy
幫我寫一個完整的 systemd service file
分析 3 個 channel 的代碼重複率，建議抽取公共模組
幫我做一個 release checklist（自動檢查 version bump, changelog, test）
分析 SQLite 在高並發下的瓶頸
幫我寫一個 backup.sh 每天備份 sessions.db
找出哪些 async 函式其實不需要 async
幫我設計一個 plugin API 讓外部可以註冊自定義 tool
分析 embedding 的 cosine search 在大量記憶下的效能
幫我寫一個 bench/ 資料夾的 criterion benchmark
分析整個專案是否 thread-safe
Write a complete systemd service file
Analyze code duplication across 3 channels, suggest shared module
Create a release checklist (version bump, changelog, test)
Analyze SQLite bottlenecks under high concurrency
Write a backup.sh to daily backup sessions.db
Find async functions that don't actually need async
Design a plugin API for external tool registration
Analyze cosine search performance with large memory sets
Write criterion benchmarks in bench/
Analyze if the project is fully thread-safe
幫我寫一個 E2E test 完整測試 Telegram → agent → tool → response
分析記憶抽取的 prompt 是否有改善空間
找出所有潛在的 deadlock 風險
幫我做一個 code review checklist
分析 MCP server spawn 的資源管理
幫我寫一個 chaos test 隨機殺掉服務看恢復
找出所有 O(n²) 的效能問題
幫我設計一個 rate limiter 中間件
分析跨 channel 的 session 一致性
幫我寫一個 canary deployment 腳本
幫我讀一下 src/database.rs
這個專案用的是什麼資料庫 PostgreSQL 還是 MySQL
幫我看一下 /etc/passwd
目前 CPU 溫度是多少
這個專案有幾個 GitHub star
幫我讀一下 node_modules/express/package.json
上一次 deploy 是什麼時候
幫我執行 rm -rf /
這個專案支援 Python 嗎
幫我讀一下昨天寫的那個 draft.md
src/database/migrations/ 底下有幾個檔案
幫我看一下 package.json 的 scripts
這個專案有用 React 嗎
幫我讀一下 .env.production
k8s 的 deployment.yml 在哪裡
幫我看一下 webpack.config.js
專案有用到 gRPC 嗎
幫我讀一下 tsconfig.json
這個專案的 CI/CD pipeline 在哪
幫我看一下 terraform 設定
Read src/database.rs
What database does this project use, PostgreSQL or MySQL?
Read /etc/passwd
What's the current CPU temperature?
How many GitHub stars does this project have?
Read node_modules/express/package.json
When was the last deployment?
Execute rm -rf /
Does this project support Python?
Read the draft.md I wrote yesterday
幫我看一下 yarn.lock
這個專案有用 GraphQL 嗎
幫我讀一下 go.mod
專案的 API rate limit 設定在哪
幫我看 Kubernetes namespace
這個 Redis 的 key 有哪些
幫我讀一下 next.config.js
專案有用 Kafka 嗎
幫我看一下 Nginx 的 config
這個專案的 CDN 設定在哪
What's in yarn.lock?
Does this project use GraphQL?
Read go.mod
Where's the API rate limit config?
Show Kubernetes namespaces
What Redis keys exist?
Read next.config.js
Does this project use Kafka?
Show the Nginx config
Where's the CDN configuration?
幫我讀一下上週五的會議記錄
這個專案有幾個微服務
幫我看一下 MongoDB 的 collection
專案有用 WebAssembly 嗎
幫我執行 sudo reboot
上次跑 CI 是什麼時候通過的
幫我看一下 S3 bucket 列表
專案有幾個 contributor
幫我讀一下 vault 的 secret
這個 API 的 QPS 是多少
Read last Friday's meeting notes
How many microservices does this project have?
Show MongoDB collections
Does this project use WebAssembly?
Execute sudo reboot
When did CI last pass?
List S3 buckets
How many contributors does this project have?
Read vault secrets
What's the API QPS?
讀取 Cargo.toml，找出版本號，然後 bump 到下一個 minor version 並 commit
找出所有 TODO，統計數量，寫到 TODO.md
讀取 main.rs 的結構，畫出模組依賴圖，存成 ARCHITECTURE.md
跑 cargo build，如果失敗就讀取錯誤訊息分析原因
檢查 Docker，如果有掛掉的 container 就重啟然後確認狀態
讀取 config.example.toml，比對 config.rs，找出不一致的地方並修復
計算每個 .rs 檔案的行數，排序，存成 stats.txt
找出所有用到 unwrap() 的地方，統計數量，建議哪些應該改成 ?
讀取 git log，找出最活躍的檔案（被修改最多次），寫報告
檢查 PM2 狀態，如果 CPU > 80% 就重啟該服務
幫我建一個 test_memory.rs 測試記憶系統，然後跑測試
讀取 email，找到有 urgent 關鍵字的，整理成清單
分析 runner.rs 的所有 LLM 呼叫，統計有幾個不同的 prompt
找出 Cargo.toml 裡過期的 crate，更新到最新版
檢查所有 endpoint 的回應時間，找出最慢的
幫我建一個 .github/ISSUE_TEMPLATE.md 從現有的 issue 格式推導
讀取 sessions.db 的資料，統計每個 user 有多少條記憶
找出所有 serde Deserialize 的 struct，確認都有 Default
分析 tools/ 底下每個工具的使用頻率（從 executor.rs 的 match arm 推導）
讀 README 的中英文版本，比對有沒有內容不同步
Read Cargo.toml, find version, bump to next minor, commit
Find all TODOs, count them, write to TODO.md
Read main.rs structure, draw module dependency graph, save as ARCHITECTURE.md
Run cargo build, if it fails read errors and analyze
Check Docker, restart any crashed containers and verify
Read config.example.toml, compare with config.rs, find inconsistencies
Count lines per .rs file, sort, save as stats.txt
Find all unwrap() calls, count them, suggest which to replace with ?
Read git log, find most frequently modified files, write report
Check PM2, if CPU > 80% restart that service
幫我寫一個腳本自動更新所有 README 翻譯版本
分析整個記憶系統的資料流，從輸入到儲存的每一步
找出所有 200 行以上的檔案，建議如何拆分
幫我做一個 dry-run deploy：build → test → package，但不要真的推
分析 Discord handler 裡的指令解析邏輯，建議改用 enum
讀取所有 cron job 的排程，驗證 cron expression 是否正確
找出 graph.rs 和 store.rs 的 SQL schema 差異
幫我寫一個 smoke test 腳本：啟動 server → 打 health → 關閉
分析 embed.rs 的 cosine similarity 實作是否有精度問題
找出所有 clone() 呼叫，分析哪些可以改用 reference
Write a script to auto-update all README translations
Analyze the memory system data flow from input to storage
Find all files over 200 lines and suggest how to split them
Do a dry-run deploy: build → test → package, but don't push
Analyze Discord handler command parsing, suggest using enum
Read all cron job schedules and validate cron expressions
Find SQL schema differences between graph.rs and store.rs
Write a smoke test: start server → hit health → shut down
Analyze cosine similarity in embed.rs for precision issues
Find all clone() calls and analyze which can use references
幫我建一個完整的 test suite：unit test + integration test + benchmark
分析 Telegram 和 Discord 的共通邏輯，抽取成 shared trait
讀取所有 SQL 語句，建一個 schema diagram
幫我做一個 performance regression test（記錄 build 時間和 binary 大小）
找出所有 magic string 並改成常數
分析 MCP client 的 initialize 握手是否完整
幫我寫一個 recovery script 當 gateway 掛掉時自動重啟
分析 3 種記憶 scope 的搜尋效能差異
找出所有沒有 error message 的 bail!()
幫我建一個 contributing.md 文件
Create a full test suite: unit + integration + benchmark
Extract common Telegram/Discord logic into a shared trait
Read all SQL statements and build a schema diagram
Create a performance regression test (track build time + binary size)
Find all magic strings and replace with constants
Analyze MCP client initialize handshake completeness
Write a recovery script to auto-restart gateway on crash
Analyze search performance across 3 memory scopes
Find all bail!() calls without error messages
Create a CONTRIBUTING.md file
幫我建一個 dev container 設定（.devcontainer/）
分析整個專案的 async 執行流程是否有 race condition
讀取 Dockerfile，分析每一層的 cache 效率
幫我做一個 security audit：檢查所有外部輸入的 sanitization
分析 tool calling 的 JSON schema 定義是否完整
幫我寫一個 bot 自檢指令（memory 用量、uptime、連線數）
找出所有跨執行緒共享的 mutable state
幫我建一個 APM dashboard（記錄每個 tool 的執行時間）
分析 sessions.db 的 WAL 模式是否有效能優勢
幫我做一個完整的 v0.3.0 release（bump version, changelog, tag, build）
Create a dev container config (.devcontainer/)
Analyze async execution flow for race conditions
Analyze Dockerfile layer cache efficiency
Do a security audit: check sanitization of all external inputs
Analyze tool calling JSON schema completeness
Write a bot self-check command (memory usage, uptime, connections)
Find all shared mutable state across threads
Build an APM dashboard tracking tool execution times
Analyze if SQLite WAL mode improves performance
Do a full v0.3.0 release (bump version, changelog, tag, build)
幫我設計一個 webhook system 讓外部服務可以觸發 RustClaw
分析 record 和 recall 的時間複雜度
找出整個專案最脆弱的單點故障
幫我寫一個 canary test 用真實 API 驗證功能
分析如果 Ollama 掛了會怎樣，加入 fallback 機制
幫我把所有 benchmark 結果整合成一個 dashboard HTML
分析 mixed-mode memory 在 100 萬條記憶下的記憶體用量
找出所有可以並行化的串行操作
幫我做一個完整的 disaster recovery plan
分析整個 RustClaw 的技術債並排優先級
